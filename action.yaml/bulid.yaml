name: steps-testing

on:
  push:
 branches: "*"

 jobs:
build-id:
runs-on: ubuntu-lastest
steps:
-name: sample data
id: version
runs:
   echo "version=1.2.3" >> "$GITHUB_OUTPUT"
   -name:print output
   run:
   echo "build version is ${{steps.version_data.output.version}}"
